import { ScrollDirectiveDirective } from './scroll-directive.directive';

describe('ScrollDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
